import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ExcelControl = () => {
  return (
    <View>
      <Text>ExcelControl</Text>
    </View>
  );
};

export default ExcelControl;

const styles = StyleSheet.create({});
