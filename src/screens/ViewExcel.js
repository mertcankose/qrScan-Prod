import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ViewExcel = ({ navigation }) => {
  return (
    <View>
      <Text>ViewExcel</Text>
    </View>
  );
};

export default ViewExcel;

const styles = StyleSheet.create({});
